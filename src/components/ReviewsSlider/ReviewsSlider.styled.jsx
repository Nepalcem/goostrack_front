import styled from 'styled-components';
import { Container } from '../App/App.styled';

export const ReviewsSliderBlock = styled.section``;

export const ReviewsSliderContainer = styled(Container)``;
